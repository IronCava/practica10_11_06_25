<%- include('partials/_head') %>

<%
  // Fallback por si no llega "name"
  const _name = (typeof name !== 'undefined' && name) ? name : 'Usuario';
%>

<section class="editor">
  <h1>Panel de editor</h1>
  <p>Bienvenid@, <strong><%= _name %></strong></p>

  <!-- Enlace a productos (modo lectura para editores) -->
  <p><a class="btn" href="/productos">Ver productos</a></p>

  <!-- Logout -->
  <p><a href="/logout" class="btn">Cerrar sesión</a></p>

  <!-- Aquí más adelante inyectaremos la tabla en modo lectura -->
  <div class="editor-content">
    <!-- Contenido específico del editor -->
  </div>
</section>

<%- include('partials/_foot') %>
